import { createTransportState, transportStep, transportTelemetry } from './transport_physics.js';
import { TRANSPORT_TYPES } from './transport_constants.js';
import { getTransportProfile, getTransportTypeProfile, resolveTransportPhysics } from './transport_profiles.js';
import { applyActuatorDelay } from './vehicle_safety.js';
import { stepCarPhysics } from './car_physics.js';
import { vehicleWorldBlocked, vehicleHitsBuilding } from './vehicle_collision.js';
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;
function calibratedPhysics(base,calibration=null){if(!calibration||calibration.ready===false)return base;const confidence=clamp(finite(calibration.confidence,0),0,1),scale=(v,f=1)=>clamp(finite(v,f),.75,1.25),a=scale(calibration.accelerationScale),b=scale(calibration.brakingScale),s=scale(calibration.steeringScale),d=scale(calibration.dragScale),r=scale(calibration.turnRadiusScale);return{...base,engineForce:base.engineForce*(1+(a-1)*confidence),brakeForce:base.brakeForce*(1+(b-1)*confidence),steeringRate:base.steeringRate*(1+(s-1)*confidence),drag:base.drag*(1+(d-1)*confidence),turnRadius:base.turnRadius*(1+(r-1)*confidence),calibration:{accelerationScale:a,brakingScale:b,steeringScale:s,dragScale:d,turnRadiusScale:r,confidence}};}
function calibrationForVehicle(profile,id){return profile&&typeof profile==='object'?profile.vehicles?.[id]||null:null;}
function calibratedCarProfile(p,physics){return{maxForwardSpeed:Math.min(p.maxSpeed||physics.maxForwardSpeed||455,physics.maxForwardSpeed||455),engineAcceleration:p.engineAcceleration||145,reverseAcceleration:p.reverseAcceleration||85,wheelbase:p.wheelbase||2.7,frontWeight:p.frontWeight||.52,cgHeight:p.cgHeight||.55,maxSteering:p.maxSteering||.58};}
function buildActivePhysics(id,type,calibration){const base=calibratedPhysics(resolveTransportPhysics(id),calibration);return type===TRANSPORT_TYPES.CAR?{...base,...calibratedCarProfile(getTransportProfile(id),base),model:'car-physics-swept'}:{...base,model:'force-based'};}
export function createTransportController(vehicleId='sedan',initial={}){
 const source=initial&&typeof initial==='object'?initial:{};const {roadLines=globalThis.__LOWTOWN_ROAD_LINES||[],vehicleLength=56,vehicleWidth=28,...stateInitial}=source;let currentId=getTransportProfile(vehicleId).id;const profile=()=>getTransportProfile(currentId);const state=createTransportState({type:profile().type,vehicleId:currentId,mass:profile().mass,...stateInitial});let calibrationProfile=null,calibrationByVehicle={};let physics=buildActivePhysics(currentId,state.type,null);state.mass=physics.mass;state.physics=physics;resetCarState(state);let lastInput={throttle:0,brake:0,steer:0,handbrake:false,climb:0,descend:0},actuator={throttle:0,brake:0,steer:0,climb:0,descend:0},actuatorResponse={steer:12,throttle:8,brake:16,climb:8,descend:8};state.actuatorResponse={...actuatorResponse};
 function resetCarState(s){s.v=finite(s.v,0);s.vx=finite(s.vx,0);s.vy=finite(s.vy,0);s.vz=finite(s.vz,0);s.yawRate=finite(s.yawRate,0);s.steerAngle=finite(s.steerAngle,0);s.distance=finite(s.distance,0);s.age=finite(s.age,0);s.handlingModel=s.handlingModel||'kinematic-grip';return s;}
 function activeCalibration(){return calibrationByVehicle[currentId]||calibrationForVehicle(calibrationProfile,currentId);}function rebuildPhysics(){physics=buildActivePhysics(currentId,state.type,activeCalibration());state.mass=physics.mass;state.physics=physics;return physics;}function setVehicle(id){const next=getTransportProfile(id);currentId=next.id;state.vehicleId=currentId;state.type=next.type;state.surface=next.type===TRANSPORT_TYPES.CAR?'road':next.type===TRANSPORT_TYPES.BOAT?'water':'air';resetActuators();resetCarState(state);rebuildPhysics();return next;}function setCalibration(next=null,vehicleId=currentId){const id=getTransportProfile(vehicleId).id;if(next&&typeof next==='object'){if(next.vehicles&&typeof next.vehicles==='object'){calibrationProfile={...next,vehicles:{...next.vehicles}};calibrationByVehicle={...calibrationByVehicle,...next.vehicles};}else calibrationByVehicle={...calibrationByVehicle,[id]:{...next}};}else if(next===null){delete calibrationByVehicle[id];if(calibrationProfile?.vehicles?.[id]){const vehicles={...calibrationProfile.vehicles};delete vehicles[id];calibrationProfile={...calibrationProfile,vehicles};}}rebuildPhysics();return activeCalibration();}function setCalibrationProfile(p={}){calibrationProfile=p&&typeof p==='object'?{...p,vehicles:{...(p.vehicles||{})}}:null;calibrationByVehicle={...(calibrationProfile?.vehicles||{})};rebuildPhysics();return calibrationProfile;}function getCalibration(vehicleId=currentId){return calibrationByVehicle[vehicleId]||calibrationProfile?.vehicles?.[vehicleId]||null;}function clearCalibration(vehicleId=currentId){const id=getTransportProfile(vehicleId).id;delete calibrationByVehicle[id];if(calibrationProfile?.vehicles?.[id]){const vehicles={...calibrationProfile.vehicles};delete vehicles[id];calibrationProfile={...calibrationProfile,vehicles};}rebuildPhysics();return physics;}function clearAllCalibration(){calibrationProfile=null;calibrationByVehicle={};rebuildPhysics();return physics;}function setActuatorResponse(next={}){actuatorResponse={...actuatorResponse,...Object.fromEntries(Object.entries(next).filter(([,v])=>Number.isFinite(Number(v))&&Number(v)>=0))};state.actuatorResponse={...actuatorResponse};return{...actuatorResponse};}function resetActuators(){actuator={throttle:0,brake:0,steer:0,climb:0,descend:0};return{...actuator};}
 function control(input={},dt=1/60){const type=state.type,target={throttle:clamp(Number(input.throttle)||0,-1,1),brake:clamp(Number(input.brake)||0,0,1),steer:clamp(Number(input.steer)||0,-1,1),handbrake:type===TRANSPORT_TYPES.CAR&&!!input.handbrake,climb:type===TRANSPORT_TYPES.PLANE?clamp(Number(input.climb)||0,-1,1):0,descend:type===TRANSPORT_TYPES.PLANE?clamp(Number(input.descend)||0,0,1):0};if(type===TRANSPORT_TYPES.CAR&&target.throttle<0)target.brake=0;if(type===TRANSPORT_TYPES.PLANE)target.throttle=Math.max(0,target.throttle);const next=applyActuatorDelay(target,actuator,clamp(finite(dt),0,.1),actuatorResponse);actuator={throttle:next.throttle,brake:next.brake,steer:next.steer,climb:next.climb,descend:next.descend};lastInput={...next,handbrake:target.handbrake};return lastInput;}
 function step(dt,input=lastInput){const safeDt=clamp(finite(dt),0,.1);control(input,safeDt);const old={x:state.x,y:state.y,z:state.z,a:state.a,v:state.v,vx:state.vx,vy:state.vy,vz:state.vz,yawRate:state.yawRate,distance:state.distance,age:state.age};let telemetry;if(state.type===TRANSPORT_TYPES.CAR){const speed=Math.hypot(Number(state.vx)||0,Number(state.vy)||0),maxSliceDistance=8,maxPhysicsStep=.05,slices=Math.max(1,Math.ceil(Math.max(speed*safeDt,maxSliceDistance*.5)/maxSliceDistance),Math.ceil(safeDt/maxPhysicsStep)),h=safeDt/slices;let blockedHit=false;for(let i=0;i<slices;i++){const before={x:state.x,y:state.y,z:state.z,a:state.a,v:state.v,vx:state.vx,vy:state.vy,vz:state.vz,yawRate:state.yawRate,distance:state.distance,age:state.age,steerAngle:state.steerAngle};stepCarPhysics(state,h,{...lastInput,surface:'road'},physics);state.distance=before.distance+Math.hypot(state.x-before.x,state.y-before.y);state.age=before.age+h;state.v=state.vx*Math.cos(state.a)+state.vy*Math.sin(state.a);const hit=vehicleWorldBlocked(state.x,state.y,state.a,roadLines,{length:vehicleLength,width:vehicleWidth,roadTolerance:8});if(hit){const buildingHit=vehicleHitsBuilding(state.x,state.y,state.a,{length:vehicleLength,width:vehicleWidth,buildingMargin:4});const candidate={x:state.x,y:state.y};state.x=before.x;state.y=before.y;state.z=before.z;state.a=before.a;state.distance=before.distance;state.age=before.age;state.steerAngle=before.steerAngle;state.v=before.v;state.vx=before.vx;state.vy=before.vy;state.yawRate=before.yawRate;const movedX=candidate.x-before.x,movedY=candidate.y-before.y;let slid=false;if(buildingHit){let lo=0,hi=1;for(let j=0;j<7;j++){const mid=(lo+hi)*.5;const tx=before.x+(candidate.x-before.x)*mid,ty=before.y+(candidate.y-before.y)*mid;if(vehicleHitsBuilding(tx,ty,before.a,{length:vehicleLength,width:vehicleWidth,buildingMargin:4}))hi=mid;else lo=mid;}const safeX=before.x+(candidate.x-before.x)*lo,safeY=before.y+(candidate.y-before.y)*lo;const dx=candidate.x-safeX,dy=candidate.y-safeY,mag=Math.hypot(dx,dy);let best=null;if(mag>.001){const base=Math.atan2(dy,dx);for(let k=-8;k<=8;k++){const ang=base+k*(Math.PI/16),step=Math.min(mag,4),tx=safeX+Math.cos(ang)*step,ty=safeY+Math.sin(ang)*step;if(vehicleHitsBuilding(tx,ty,before.a,{length:vehicleLength,width:vehicleWidth,buildingMargin:4}))continue;const score=(dx*Math.cos(ang)+dy*Math.sin(ang))/mag;if(!best||score>best.score)best={tx,ty,ang,score};}}state.x=best?best.tx:safeX;state.y=best?best.ty:safeY;const moveX=state.x-before.x,moveY=state.y-before.y,moveMag=Math.hypot(moveX,moveY);if(best&&moveMag>.001){const speed=Math.hypot(before.vx,before.vy);const dirX=moveX/moveMag,dirY=moveY/moveMag;const along=before.vx*dirX+before.vy*dirY;state.vx=dirX*Math.max(0,along);state.vy=dirY*Math.max(0,along);state.v=state.vx*Math.cos(state.a)+state.vy*Math.sin(state.a);state.yawRate*=.82;state.distance=before.distance+moveMag;}else{state.vx=0;state.vy=0;state.v=0;state.yawRate=0;state.distance=before.distance+Math.hypot(state.x-before.x,state.y-before.y);}blockedHit=true;break;}if(Math.abs(movedX)>.001){state.x=candidate.x;if(!vehicleWorldBlocked(state.x,state.y,state.a,roadLines,{length:vehicleLength,width:vehicleWidth,roadTolerance:8})){state.vx*=.82;state.vy*=.96;slid=true;}else state.x=before.x;}if(!slid&&Math.abs(movedY)>.001){state.y=candidate.y;if(!vehicleWorldBlocked(state.x,state.y,state.a,roadLines,{length:vehicleLength,width:vehicleWidth,roadTolerance:8})){state.vx*=.96;state.vy*=.82;slid=true;}else state.y=before.y;}if(!slid){state.vx*=.08;state.vy*=.08;state.v=state.vx*Math.cos(state.a)+state.vy*Math.sin(state.a);state.yawRate*=.35;}else{state.vx*=.9;state.vy*=.9;state.v=state.vx*Math.cos(state.a)+state.vy*Math.sin(state.a);state.yawRate*=.65;}blockedHit=true;break;}}
 state.surface='road';telemetry={x:state.x,y:state.y,heading:state.a,velocity:Math.hypot(state.vx,state.vy),forwardSpeed:state.v,lateralSpeed:-state.vx*Math.sin(state.a)+state.vy*Math.cos(state.a),yawRate:state.yawRate||0,slipAngle:0,traction:1,surface:'road',distance:state.distance||0,drift:false,controls:{...lastInput},collisionBlocked:blockedHit,handlingModel:state.handlingModel||'kinematic-grip',modelBlend:state.modelBlend||0};}else telemetry=transportStep(state,safeDt,lastInput);const dx=state.x-old.x,dy=state.y-old.y,dz=state.z-old.z;telemetry.frameDistance=Math.hypot(dx,dy,dz);telemetry.vehicleId=currentId;telemetry.physics={type:physics.type,mass:physics.mass,maxSpeed:physics.maxForwardSpeed,engineForce:physics.engineForce,brakeForce:physics.brakeForce,steeringRate:physics.steeringRate,grip:physics.lateralGrip,turnRadius:physics.turnRadius,calibration:physics.calibration||null,model:physics.model};telemetry.calibration=activeCalibration()?{...activeCalibration()}:null;telemetry.actuator={target:{...input},applied:{...lastInput},response:{...actuatorResponse}};state.telemetry=telemetry;state.actuatorState={...actuator};return telemetry;}
 function snapshot(){const v=profile(),t=getTransportTypeProfile(v.type);return{vehicleId:v.id,profile:v,typeProfile:t,state:{...state},physics:{...physics},calibration:activeCalibration()?{...activeCalibration()}:null,calibrationProfile:calibrationProfile?{...calibrationProfile,vehicles:{...(calibrationProfile.vehicles||{})}}:null,input:{...lastInput},actuator:{...actuator},actuatorResponse:{...actuatorResponse},telemetry:transportTelemetry(state,lastInput,physics)};}
 return{get vehicleId(){return currentId;},get profile(){return profile();},get typeProfile(){return getTransportTypeProfile(state.type);},get state(){return state;},get physics(){return physics;},get calibration(){return activeCalibration();},setVehicle,setCalibration,setCalibrationProfile,getCalibration,clearCalibration,clearAllCalibration,setActuatorResponse,resetActuators,control,step,snapshot};
}
