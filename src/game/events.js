export function createEventSystem({nodes,seed=9001}){let rngState=seed>>>0,active=null,cooldown=5;const types=[{id:'CRASH',title:'ACCIDENT',duration:8},{id:'CHASE',title:'POLICE CHASE',duration:12},{id:'STREET',title:'STREET RACE',duration:15},{id:'FOOT',title:'FOOT CHASE',duration:9},{id:'BLOCK',title:'ROAD BLOCK',duration:10}];const rand=()=>{rngState=(rngState*1664525+1013904223)>>>0;return rngState/4294967296},pick=()=>nodes[(rand()*nodes.length)|0];function spawn(){const t=types[(rand()*types.length)|0],n=pick();active={...t,x:n.x,y:n.y,timer:t.duration,intensity:.5+rand()*.5}}function update(dt,player){cooldown-=dt;if(active){active.timer-=dt;if(active.timer<=0){active=null;cooldown=4+rand()*8}}else if(cooldown<=0&&rand()<dt*.035)spawn();if(active&&player)active.distance=Math.hypot(player.x-active.x,player.y-active.y)}function state(){return active?{...active}:null}function draw(ctx,iso){if(!active)return;const p=iso(active.x,active.y);ctx.save();ctx.strokeStyle=active.id==='CHASE'?'#d4523a':'#e09a3e';ctx.lineWidth=3;ctx.setLineDash([7,6]);ctx.beginPath();ctx.arc(p.x,p.y,28+Math.sin(active.timer*5)*4,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.fillStyle='#e7e2d8';ctx.font='bold 10px monospace';ctx.textAlign='center';ctx.fillText(active.title,p.x,p.y-36);ctx.restore()}return{update,draw,state}}